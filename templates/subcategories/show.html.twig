{% extends 'base.html.twig' %}

{% block title %}Subcategories{% endblock %}

{% block body %}
    <h1>Subcategories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subcategory.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ subcategory.name }}</td>
            </tr>
            <tr>
                <th>Image_url</th>
                <td>{{ subcategory.imageUrl }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_subcategories_index') }}">back to list</a>

    <a href="{{ path('app_subcategories_edit', {'id': subcategory.id}) }}">edit</a>

    {{ include('subcategories/_delete_form.html.twig') }}
{% endblock %}
