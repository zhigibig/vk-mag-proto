<form method="post" action="{{ path('app_warehouses_delete', {'id': warehouse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ warehouse.id) }}">
    <button class="btn">Delete</button>
</form>
