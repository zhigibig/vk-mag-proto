{% extends 'base.html.twig' %}

{% block title %}Warehouses{% endblock %}

{% block body %}
    <h1>Warehouses</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ warehouse.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ warehouse.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_warehouses_index') }}">back to list</a>

    <a href="{{ path('app_warehouses_edit', {'id': warehouse.id}) }}">edit</a>

    {{ include('warehouses/_delete_form.html.twig') }}
{% endblock %}
